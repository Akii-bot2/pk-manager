# ポケカコミュニティ管理システム 要件定義書

**ver 2.0 | 2026年3月**  
参照：[チャンピオンシップシリーズ2026 CSP・賞品について（公式）](https://www.pokemon-card.com/info/005160.html)

---

## 1. プロジェクト概要

Discordコミュニティ（数十名規模）のポケモンカード大会管理を効率化するWebアプリ。  
メンバーが自分で参加予定・成績を入力し、コミュニティ全体でリアルタイムにCSPランキングや参加予定を把握できる。

### 解決する課題

| # | 課題 |
|---|------|
| 1 | 管理者がDiscordスレッドを目視で追いExcelに手作業転記する負担 |
| 2 | シティリーグの日程変更への対応が追いつかない |
| 3 | CSP集計・コミュニティ内ランキングをリアルタイムに把握できない |

---

## 2. 技術スタック

| 役割 | 採用技術 |
|------|----------|
| バックエンド | Python / FastAPI |
| フロントエンド | HTML + Tailwind CSS + Alpine.js |
| データベース | SQLite（開発）→ PostgreSQL（本番） |
| ホスティング | Render（無料プラン・GitHub連携で自動デプロイ） |
| バージョン管理 | GitHub |

---

## 3. ユーザー種別と権限

| 種別 | 識別方法 | できること |
|------|----------|------------|
| 一般メンバー | 登録した名前を選択 | 自分のデータの登録・編集・削除 |
| 管理者 | 管理者パスワード入力 | 全メンバーのデータ編集・削除、シーズン設定 |

- 管理者パスワードは環境変数（`ADMIN_PASSWORD`）で管理
- ログイン状態はセッションまたはローカルストレージで保持

---

## 4. 機能一覧（Phase 1）

### 4-1. メンバー登録

- 名前を入力して登録（初回のみ）
- 登録した名前はコミュニティ内でユニーク
- 自分の名前は自分で編集・削除可能
- 管理者はすべてのメンバーを編集・削除可能

### 4-2. シティリーグ

#### 参加予定の登録

- シーズン（S1〜S4、管理者が年度ごとに設定）を選択
- 参加する場合：日付・会場を入力して登録
- 不参加の場合：「不参加」にチェックして登録
- 登録後は自分で編集・削除可能

#### 大会結果の入力（大会後）

- 対象シーズンを選択
- 入力項目：
  - 順位（必須）
  - 参加人数（必須、CSP自動計算に使用）
  - デッキコード（任意）
- 入力するとCSPが自動計算されて反映される

### 4-3. トレーナーズリーグ

- 日付・会場の入力は不要
- 入力項目：
  - 参加人数（必須）
  - 順位（必須）
- 年間上限30ptを超えた分は加算されない（自動で上限チェック）

### 4-4. チャンピオンズリーグ（CL）

- CSP付与なし。記録および出場権判定のみ。
- 入力項目：
  - CL名（例：CL横浜2026）（必須）
  - 参加リーグ（必須）：マスター / シニア / ジュニア / オープン
  - 順位（任意）
  - 勝数（任意、出場権自動判定に使用）
  - 敗数（任意）
  - デッキコード（任意）
- 入力後、7章の判定ロジックに基づき出場権取得済みバッジを自動表示

### 4-5. CSPランキング一覧

- コミュニティ内メンバーのCSP合計を降順で表示
- 出場権ラインに達しているメンバーに👑マークを表示

| 👑マーク | 対象ライン（マスターリーグ基準） |
|----------|-------------------------------|
| 👑🌍 WCS | CSPランキング上位40位以内 |
| 👑🇯🇵 JCS | CSPランキング上位200位以内 |
| 👑🎯 次回CL | CSPランキング上位40位以内 |

> ⚠️ コミュニティ内順位と全国順位は異なる。アプリ内の表示は「目安」として扱い、その旨を明記する。

### 4-6. 個人CSPランキング詳細

- 獲得CSP内訳（シティS1〜S4、トレリ合計）
- 現在のCSP合計と、各出場権ラインまでの残りpt
- 獲得済み賞品ライン（30pt / 80pt）の表示
- CL参加履歴一覧

### 4-7. 大会参加カレンダー（シティリーグ）

- 月別カレンダーで各メンバーのシティリーグ参加予定を表示
- 「不参加」登録のメンバーはグレーアウト表示
- 参加予定日をタップすると会場名などの詳細を表示

---

## 5. データベース設計

### members（メンバー）

| カラム名 | 型 | 制約 | 説明 |
|----------|----|------|------|
| id | INTEGER | PK, AUTOINCREMENT | メンバーID |
| name | TEXT | NOT NULL, UNIQUE | 表示名 |
| created_at | TEXT | NOT NULL | 登録日時（ISO8601） |

### seasons（シーズンマスタ）

| カラム名 | 型 | 制約 | 説明 |
|----------|----|------|------|
| id | INTEGER | PK, AUTOINCREMENT | シーズンID |
| year | INTEGER | NOT NULL | 年度（例：2026） |
| season_number | INTEGER | NOT NULL | シーズン番号（1〜4） |
| name | TEXT | NOT NULL | 表示名（例：2026 S2） |

### city_entries（シティリーグ参加予定・結果）

| カラム名 | 型 | 制約 | 説明 |
|----------|----|------|------|
| id | INTEGER | PK, AUTOINCREMENT | エントリーID |
| member_id | INTEGER | FK → members | メンバーID |
| season_id | INTEGER | FK → seasons | シーズンID |
| is_absent | INTEGER | DEFAULT 0 | 不参加フラグ（1=不参加） |
| date | TEXT | NULL可 | 大会日（YYYY-MM-DD） |
| venue | TEXT | NULL可 | 会場名 |
| rank | INTEGER | NULL可 | 最終順位 |
| participants_count | INTEGER | NULL可 | 参加人数（CSP計算用） |
| deck_code | TEXT | NULL可 | デッキコード（任意） |
| csp_earned | INTEGER | NULL可 | 獲得CSP（自動計算） |
| updated_at | TEXT | NOT NULL | 最終更新日時 |

> 制約：`(member_id, season_id)` にユニーク制約（1シーズン1エントリーのみ）

### trainer_entries（トレーナーズリーグ実績）

| カラム名 | 型 | 制約 | 説明 |
|----------|----|------|------|
| id | INTEGER | PK, AUTOINCREMENT | エントリーID |
| member_id | INTEGER | FK → members | メンバーID |
| rank | INTEGER | NOT NULL | 順位 |
| participants_count | INTEGER | NOT NULL | 参加人数 |
| csp_earned | INTEGER | NOT NULL | 獲得CSP（自動計算・上限考慮済み） |
| created_at | TEXT | NOT NULL | 入力日時 |

### cl_entries（CL参加記録）

| カラム名 | 型 | 制約 | 説明 |
|----------|----|------|------|
| id | INTEGER | PK, AUTOINCREMENT | エントリーID |
| member_id | INTEGER | FK → members | メンバーID |
| cl_name | TEXT | NOT NULL | CL名（例：CL横浜2026） |
| league | TEXT | NOT NULL | 参加リーグ（master / senior / junior / open） |
| rank | INTEGER | NULL可 | 最終順位 |
| wins | INTEGER | NULL可 | 勝数（出場権判定に使用） |
| losses | INTEGER | NULL可 | 敗数 |
| deck_code | TEXT | NULL可 | デッキコード（任意） |
| created_at | TEXT | NOT NULL | 入力日時 |

---

## 6. CSP自動計算ロジック

> 出典：[チャンピオンシップシリーズ2026 CSP・賞品について（公式）](https://www.pokemon-card.com/info/005160.html)

### 6-1. シティリーグ

順位と参加人数の組み合わせでCSPが決まる。

| 獲得CSP | 成績（順位） | 参加者数 |
|---------|------------|---------|
| 100 | 1位 | 1〜3人 |
| 75 | 2位 | 4〜8人 |
| 50 | 3〜4位 | 9〜16人 |
| 25 | 5〜8位 | 17〜32人 |
| 15 | 9〜16位 | 33〜64人 |

```python
CITY_CSP_TABLE = [
    # (rank_limit, participants_min, participants_max, csp)
    (1,  1,  3,  100),
    (2,  4,  8,   75),
    (4,  9,  16,  50),
    (8,  17, 32,  25),
    (16, 33, 64,  15),
]

def calc_city_csp(rank: int, participants: int) -> int:
    for rank_limit, p_min, p_max, csp in CITY_CSP_TABLE:
        if rank <= rank_limit and p_min <= participants <= p_max:
            return csp
    return 0
```

### 6-2. トレーナーズリーグ

順位と参加人数の組み合わせでCSPが決まる。年間上限30pt。

| 獲得CSP | 成績（順位） | 参加者数 |
|---------|------------|---------|
| 15 | 1位 | 2人 |
| 12 | 2位 | 3〜7人 |
| 10 | 3〜4位 | 8〜12人 |
| 8 | 5〜8位 | 13〜25人 |
| 6 | 9〜16位 | 26〜40人 |
| 5 | 17〜32位 | 41〜55人 |
| 4 | 33〜48位 | 56〜89人 |

```python
TRAINER_CSP_TABLE = [
    # (rank_limit, participants_min, participants_max, csp)
    (1,  2,  2,  15),
    (2,  3,  7,  12),
    (4,  8,  12, 10),
    (8,  13, 25,  8),
    (16, 26, 40,  6),
    (32, 41, 55,  5),
    (48, 56, 89,  4),
]

def calc_trainer_csp(rank: int, participants: int, current_total: int) -> int:
    """current_total: そのメンバーの今シーズンのトレリCSP累計"""
    ANNUAL_LIMIT = 30
    for rank_limit, p_min, p_max, csp in TRAINER_CSP_TABLE:
        if rank <= rank_limit and p_min <= participants <= p_max:
            remaining = ANNUAL_LIMIT - current_total
            return max(0, min(csp, remaining))  # 上限超過分はカット
    return 0
```

### 6-3. CL

CSP付与なし。記録のみ。計算処理は不要。

---

## 7. 出場権ラインと👑マーク仕様

> 出典：[チャンピオンシップシリーズ2026 CSP・賞品について（公式）](https://www.pokemon-card.com/info/005160.html)  
> ⚠️ CSPランキング基準はコミュニティ内順位であり全国順位とは異なる。アプリ内では目安として表示し、その旨を明記する。

出場権の取得経路は **①CSPランキング** と **②CL成績** の2種類がある。

---

### 7-1. JCS2026（ジャパンチャンピオンシップス）優先出場権

#### ① CSPランキング経由
基準日：シティリーグ2026 S4最終日翌日時点（2026年5月上旬予定）

| リーグ | 全国基準 |
|--------|---------|
| マスターリーグ | 上位200位以内 |
| シニアリーグ | 上位150位以内 |
| ジュニアリーグ | 上位100位以内 |

#### ② CL成績経由
CL2026の各大会で以下の条件を達成したプレイヤーに付与。

| リーグ | 条件 |
|--------|------|
| マスターリーグ | 予選＋本戦①通算 **12勝以上** |
| シニアリーグ | 最終順位 **上位32名** |
| ジュニアリーグ | 最終順位 **上位32名** |
| オープンリーグ | 最終順位 **上位32名** |

#### ③ シティリーグ経由
シティリーグ2026 S1〜S4の**各大会優勝者**に付与。

---

### 7-2. CL2027各大会 優先出場権

#### ① CSPランキング経由
基準日：シティリーグ2026 S4最終日翌日時点（2026年5月上旬予定）

| リーグ | 全国基準 |
|--------|---------|
| マスターリーグ | 上位40位以内 |
| シニアリーグ | 上位30位以内 |
| ジュニアリーグ | 上位30位以内 |

#### ② CL成績経由
CL2026の各大会で以下の条件を達成したプレイヤーに付与。

| リーグ | 条件 |
|--------|------|
| マスターリーグ | 最終順位 **上位4名** |
| シニアリーグ | 最終順位 **上位4名** |
| ジュニアリーグ | 最終順位 **上位4名** |
| オープンリーグ | 最終順位 **上位4名** |

---

### 7-3. WCS2026（ワールドチャンピオンシップス）出場権

#### ① CSPランキング経由
基準日：シティリーグ2026 S4最終日翌日時点（2026年5月上旬予定）

| リーグ | 全国基準 |
|--------|---------|
| マスターリーグ | 上位40位以内 |
| シニアリーグ | 上位30位以内 |
| ジュニアリーグ | 上位30位以内 |

#### ② CL成績経由
CL2026の各大会で以下の条件を達成したプレイヤーに付与。

| リーグ | 条件 |
|--------|------|
| マスターリーグ | 最終順位 **上位4名** |
| シニアリーグ | 最終順位 **上位4名** |
| ジュニアリーグ | 最終順位 **上位4名** |
| オープンリーグ | 最終順位 **上位4名** |

---

### 7-4. ランキング一覧の👑マーク仕様

👑マークは **CSPランキング経由** の目安ラインに基づいて表示する。  
CL成績経由の出場権は個人詳細ページで別途フラグ表示する。

| マーク | 出場権 | コミュニティ内表示の基準 |
|--------|--------|------------------------|
| 👑🌍 WCS | WCS出場権 | CSP上位40位ライン相当 |
| 👑🇯🇵 JCS | JCS優先出場権 | CSP上位200位ライン相当 |
| 👑🎯 次回CL | CL2027優先出場権 | CSP上位40位ライン相当 |

> マークはコミュニティ内CSP順位を全国基準に当てはめた目安表示。実際の権利は公式プレイヤーズクラブで確認が必要。

---

### 7-5. 個人詳細ページの出場権表示

#### CSPランキング経由（目安）
現在のCSP合計と各ラインの残りptを表示する。

#### CL成績経由（実績フラグ）
CL記録入力（順位・勝数）をもとに、出場権取得条件を満たしているか自動判定してバッジ表示する。

| 判定ロジック | 表示 |
|-------------|------|
| マスター：wins >= 12 | ✅ JCS優先出場権（CL経由） |
| マスター：rank <= 4 | ✅ WCS出場権 / CL2027優先出場権（CL経由） |
| シニア/ジュニア/オープン：rank <= 32 | ✅ JCS優先出場権（CL経由） |
| シニア/ジュニア/オープン：rank <= 4 | ✅ WCS出場権 / CL2027優先出場権（CL経由） |

> リーグ区分（マスター/シニア/ジュニア/オープン）はCL記録入力時にメンバーが選択する。

---

### 7-6. CSP賞品ライン

| CSP合計 | 表示内容 |
|---------|---------|
| 30pt以上 | 🎁 プロモカード1枚獲得ライン到達 |
| 80pt以上 | 🎁🎁 プロモカード2枚獲得ライン到達 |

---

## 8. API設計

| メソッド | エンドポイント | 認証 | 説明 |
|----------|---------------|------|------|
| GET | `/api/members` | 不要 | メンバー一覧取得 |
| POST | `/api/members` | 不要 | メンバー登録（名前） |
| PUT | `/api/members/{id}` | 本人 or 管理者 | メンバー名編集 |
| DELETE | `/api/members/{id}` | 本人 or 管理者 | メンバー削除 |
| GET | `/api/seasons` | 不要 | シーズン一覧取得 |
| POST | `/api/seasons` | 管理者のみ | シーズン追加 |
| DELETE | `/api/seasons/{id}` | 管理者のみ | シーズン削除 |
| GET | `/api/city` | 不要 | シティリーグ全エントリー取得 |
| POST | `/api/city` | 本人 | 参加予定・結果登録 |
| PUT | `/api/city/{id}` | 本人 or 管理者 | 編集 |
| DELETE | `/api/city/{id}` | 本人 or 管理者 | 削除 |
| GET | `/api/trainer` | 不要 | トレリ全エントリー取得 |
| POST | `/api/trainer` | 本人 | トレリ結果登録（CSP自動計算） |
| DELETE | `/api/trainer/{id}` | 本人 or 管理者 | 削除 |
| GET | `/api/cl` | 不要 | CL全エントリー取得 |
| POST | `/api/cl` | 本人 | CL記録登録 |
| PUT | `/api/cl/{id}` | 本人 or 管理者 | 編集 |
| DELETE | `/api/cl/{id}` | 本人 or 管理者 | 削除 |
| GET | `/api/ranking` | 不要 | CSPランキング一覧（👑マーク付き） |
| GET | `/api/members/{id}/detail` | 不要 | 個人詳細（CSP内訳・出場権・賞品ライン） |
| POST | `/api/admin/login` | — | 管理者パスワード認証 |

---

## 9. 画面一覧

| 画面名 | URL | 主な内容 |
|--------|-----|---------|
| トップ | `/` | CSPランキング一覧（👑マーク付き） |
| 個人詳細 | `/member/{id}` | CSP内訳・出場権・賞品ライン・CL履歴 |
| シティ参加予定入力 | `/city` | シーズン選択・日付会場入力 or 不参加チェック |
| シティ結果入力 | `/city/{entry_id}/result` | 順位・参加人数・デッキコード入力 |
| トレリ結果入力 | `/trainer` | 参加人数・順位入力 |
| CL記録入力 | `/cl` | CL名・勝敗・順位・デッキコード入力 |
| カレンダー | `/calendar` | 月別・シティリーグ参加予定一覧 |
| 管理者画面 | `/admin` | シーズン設定・メンバー管理 |

---

## 10. バリデーションルール

| 対象 | ルール |
|------|--------|
| メンバー名 | 空白不可・重複不可 |
| シティリーグ | 同一メンバー×同一シーズンで2件目はエラー |
| トレリ参加人数 | 2人未満は不可（大会不成立） |
| トレリCSP上限 | 年間累計30ptを超えた分は自動カット・画面上に警告表示 |
| CLのCSP | 計算しない（記録のみ） |

---

## 11. フォルダ構成

```
pokeca-manager/
  app/
    main.py               # FastAPIエントリーポイント
    database.py           # DB接続・初期化
    models.py             # SQLAlchemyモデル
    auth.py               # 管理者パスワード認証
    routers/
      members.py
      seasons.py
      city.py
      trainer.py
      cl.py
      ranking.py
    utils/
      csp.py              # CSP自動計算ロジック（6章）
  static/
    index.html            # トップ（CSPランキング）
    member.html           # 個人詳細
    city.html             # シティ入力
    trainer.html          # トレリ入力
    cl.html               # CL入力
    calendar.html         # カレンダー
    admin.html            # 管理者画面
    css/
    js/
  requirements.txt
  README.md
```

---

## 12. セットアップ手順

### ローカル開発

```bash
pip install fastapi uvicorn sqlalchemy aiofiles python-multipart
uvicorn app.main:app --reload
# → http://localhost:8000
```

### 環境変数

| 変数名 | 内容 |
|--------|------|
| `ADMIN_PASSWORD` | 管理者パスワード |
| `DATABASE_URL` | 本番用DB接続URL（Render PostgreSQL） |

### Renderへのデプロイ

1. GitHubにpush
2. RenderとGitHubリポジトリを連携
3. 環境変数を設定
4. 自動ビルド・デプロイ完了

> Renderの無料プランは15分アクセスがないと自動スリープする。下記のUptimeRobot設定で常時稼働させること。

### UptimeRobotによる常時稼働設定

UptimeRobotはクラウド上で動く外部サービスのため、**PCの電源を切っていても24時間365日自動で稼働し続ける**。設定は最初の1回のみ。

#### ① アカウント作成

1. [https://uptimerobot.com](https://uptimerobot.com) にアクセス
2. 「Register for FREE」からメールアドレスとパスワードで登録
3. 届いた確認メールからアカウントを有効化してログイン

#### ② 監視モニターの追加

1. ダッシュボード左上の「＋ Add New Monitor」をクリック
2. 以下の通り設定する

| 項目 | 設定値 |
|------|--------|
| Monitor Type | HTTP(s) |
| Friendly Name | ポケカ管理アプリ（任意） |
| URL | `https://（RenderのURL）/api/members` |
| Monitoring Interval | 5 minutes |

3. 「Create Monitor」をクリックして完了

#### ③ 動作確認

- ダッシュボードに追加したモニターが「Up」と緑色で表示されれば設定完了
- 以降は5分ごとに自動でアクセスし、Renderのスリープを防ぐ
- アプリがダウンした場合は登録メールアドレスに自動で通知が届く

> **注意：** RenderのURLはデプロイ完了後に `https://（アプリ名）.onrender.com` の形式で発行される。Renderのダッシュボードで確認してからUptimeRobotに設定すること。
